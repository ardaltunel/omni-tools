const valorantCrosshairs = [
    {
        name: "Artı Cross",
        category: "Klasik",
        image: "valorant-crosshair/images/1.png",
        code: "0;s;1;P;u;000000FF;h;0;f;0;0l;3;0v;3;0o;0;0a;1;0f;0;1b;0;S;b;1;c;8"
    },
    {
        name: "Nokta Cross",
        category: "Minimal",
        image: "valorant-crosshair/images/2.png",
        code: "0;s;1;P;o;1;d;1;f;0;0l;1;0o;1;0a;1;0f;0;1b;0;S;b;1;c;8"
    },
    {
        name: "Kare Cross",
        category: "Minimal",
        image: "valorant-crosshair/images/3.png",
        code: "0;s;1;P;o;1;d;1;f;0;0b;0;1b;0;S;c;0"
    },
    {
        name: "4:3 ama değil",
        category: "Özel",
        image: "valorant-crosshair/images/4.png",
        code: "0;s;1;P;h;0;f;0;0t;3;0l;0;0v;4;0g;1;0o;1;0a;0.616;0f;0;1t;1;1l;6;1v;3;1g;1;1o;2;1a;1;1m;0;1f;0;S;c;0"
    },
    {
        name: "Reyna Flash",
        category: "Eğlenceli",
        image: "valorant-crosshair/images/5.png",
        code: "0;P;c;6;t;6;o;0.3;f;0;0t;1;0l;5;0o;5;0a;1;0f;0;1t;10;1l;4;1o;5;1a;0.5;1m;0;1f;0"
    },
    {
        name: "Glasses",
        category: "Eğlenceli",
        image: "valorant-crosshair/images/6.png",
        code: "0;P;t;2;o;1;d;1;0t;10;0l;19;0v;0;0g;1;0o;1;0a;0;0e;0;1l;10;1v;0;1g;1;1o;19;1a;0;1s;0;1e;0"
    },
    {
        name: "Smiley",
        category: "Eğlenceli",
        image: "valorant-crosshair/images/7.png",
        code: "0;P;c;7;t;2;o;1;d;1;z;3;a;0;f;0;0t;10;0l;2;0o;2;0a;1;0f;0;1b;0"
    },
    {
        name: "Flappy",
        category: "Eğlenceli",
        image: "valorant-crosshair/images/8.png",
        code: "0;c;1;s;1;P;c;8;u;008000FF;t;3;o;1;b;1;0t;4;0l;0;0v;18;0g;1;0o;10;0a;1;0f;0;1t;10;1l;0;1v;4;1g;1;1o;7;1a;1;1m;0;1f;0"
    },
    {
        name: "Clown",
        category: "Eğlenceli",
        image: "valorant-crosshair/images/9.png",
        code: "0;P;c;7;t;2;o;1;d;1;z;3;f;0;0t;10;0l;2;0o;2;0a;1;0f;0;1b;0"
    },
    {
        name: "Windows",
        category: "Özel",
        image: "valorant-crosshair/images/10.png",
        code: "0;P;o;1;d;1;z;6;0b;0;1t;6;1l;3;1o;0;1a;0;1m;0;1f;0"
    },
    {
        name: "Steve",
        category: "Özel",
        image: "valorant-crosshair/images/11.png",
        code: "0;P;c;5;t;2;o;1;0t;6;0l;4;0v;3;0g;1;0o;0;0a;1;0f;0;1t;6;1v;6;1g;1;1o;5;1a;1;1m;0;1f;0"
    },
    {
        name: "Among Us",
        category: "Özel",
        image: "valorant-crosshair/images/12.png",
        code: "0;P;c;5;t;3;o;1;f;0;m;1;0t;4;0l;5;0o;0;0a;1;0f;0;1t;8;1l;3;1o;0;1a;1;1m;0;1f;0"
    },
    {
        name: "Pussy",
        category: "Eğlenceli",
        image: "valorant-crosshair/images/13.png",
        code: "0;P;c;8;u;F23591FF;o;0.355;d;1;b;1;z;1;f;0;0t;3;0l;3;0v;7;0g;1;0o;0;0a;1;0f;0;1t;9;1v;0;1g;1;1o;0;1a;1;1m;0;1f;0"
    },
    {
        name: "Windmill",
        category: "Özel",
        image: "valorant-crosshair/images/14.png",
        code: "0;P;c;1;t;6;o;1;d;1;z;6;a;0;f;0;m;1;0t;10;0l;20;0o;20;0a;1;0m;1;0e;0.1;1t;10;1l;10;1o;40;1a;1;1m;0"
    },
    {
        name: "Church Crosses",
        category: "Özel",
        image: "valorant-crosshair/images/15.png",
        code: "0;P;h;0;d;1;z;3;f;0;0t;3;0l;14;0o;6;0a;1;0f;0;1t;9;1l;3;1o;12;1a;1;1m;0;1f;0"
    },
    {
        name: "Fishnet",
        category: "Özel",
        image: "valorant-crosshair/images/16.png",
        code: "0;P;o;1;d;1;a;0;f;0;0t;8;0l;2;0o;5;0a;0;0f;0;1l;8;1o;2;1a;0;1m;0;1f;0"
    },
    {
        name: "Aşkımızın Meyvesi",
        category: "Eğlenceli",
        image: "valorant-crosshair/images/17.png",
        code: "0;s;1;P;u;8470FFFF;h;0;f;0;0t;3;0l;0;0v;3;0g;1;0o;0;0a;0.626;0f;0;1t;1;1l;4;1g;1;1o;1;1a;1;1m;0;1f;0;S;c;0"
    },
    {
        name: "Kare (çok özel)",
        category: "Minimal",
        image: "valorant-crosshair/images/18.png",
        code: "0;s;1;P;u;000000FF;o;0.8;d;1;f;0;0l;1;0v;0;0g;1;0o;1;0a;1;0f;0;1b;0;S;c;0;t;0000FFFF"
    }
];

const valorantGrid = document.getElementById("valorant-crosshair-grid");
const valorantCodeInput = document.getElementById("valorant-crosshair-code");
const valorantPreview = document.getElementById("valorant-crosshair-preview");
const valorantPreviewName = document.getElementById("valorant-crosshair-preview-name");
const valorantCopyButton = document.getElementById("valorant-crosshair-copy");
const valorantClearButton = document.getElementById("valorant-crosshair-clear");
const valorantFeedback = document.getElementById("valorant-crosshair-feedback");
let valorantFeedbackTimer;
let valorantSelectedImage = "";

function initValorantCrosshair() {
    if (!valorantGrid) return;
    setValorantSelection(valorantCrosshairs[0]);
    renderValorantCards();
    valorantCodeInput.addEventListener("input", () => {
        valorantSelectedImage = "";
        valorantPreviewName.textContent = "Özel Kod";
        renderValorantPreview(valorantCodeInput.value);
    });
    valorantCopyButton.addEventListener("click", () => copyValorantCode(valorantCodeInput.value));
    valorantClearButton.addEventListener("click", () => {
        valorantCodeInput.value = "";
        valorantSelectedImage = "";
        valorantPreviewName.textContent = "Özel Kod";
        renderValorantPreview("");
        valorantCodeInput.focus();
    });
}

function renderValorantCards() {
    valorantGrid.innerHTML = "";

    valorantCrosshairs.forEach((item) => {
        const card = document.createElement("article");
        card.className = "valorant-crosshair-card";
        card.innerHTML = `
            <button class="valorant-crosshair-card-preview" type="button" aria-label="${escapeHtml(item.name)} önizlemesini seç">
                ${createValorantImageMarkup(item)}
            </button>
            <div class="valorant-crosshair-card-body">
                <h3>${escapeHtml(item.name)}</h3>
                <p>${escapeHtml(item.code)}</p>
            </div>
            <button class="primary-button valorant-crosshair-card-copy" type="button">Kodu Kopyala</button>
        `;
        card.querySelector(".valorant-crosshair-card-preview").addEventListener("click", () => setValorantSelection(item));
        card.querySelector(".valorant-crosshair-card-copy").addEventListener("click", () => copyValorantCode(item.code, item));
        valorantGrid.appendChild(card);
    });
}

function setValorantSelection(item) {
    valorantCodeInput.value = item.code;
    valorantSelectedImage = item.image;
    valorantPreviewName.textContent = item.name;
    renderValorantPreview(item.code, item.image, item.name);
}

function renderValorantPreview(code, image = valorantSelectedImage, name = valorantPreviewName.textContent) {
    const cleanCode = code.trim();
    valorantPreview.innerHTML = cleanCode && image
        ? createValorantImageMarkup({ image, name })
        : cleanCode
            ? createValorantPreviewMarkup(cleanCode)
        : '<span class="valorant-crosshair-preview-empty">Kod girildiğinde önizleme burada görünür.</span>';
}

function createValorantImageMarkup(item) {
    return `<img src="${escapeHtml(item.image)}" alt="${escapeHtml(item.name)} crosshair önizlemesi" loading="lazy">`;
}

function createValorantPreviewMarkup(code) {
    const settings = parseValorantCrosshairCode(code);
    const color = getValorantCrosshairColor(settings);
    const innerLength = clampNumber(readValorantNumber(settings, "0l", 4), 0, 24);
    const innerVertical = clampNumber(readValorantNumber(settings, "0v", innerLength), 0, 24);
    const innerOffset = clampNumber(readValorantNumber(settings, "0o", 2), 0, 32);
    const innerThickness = clampNumber(readValorantNumber(settings, "0t", 2), 1, 12);
    const innerOpacity = clampNumber(readValorantNumber(settings, "0a", 1), 0.08, 1);
    const outerLength = clampNumber(readValorantNumber(settings, "1l", 0), 0, 24);
    const outerVertical = clampNumber(readValorantNumber(settings, "1v", outerLength), 0, 24);
    const outerOffset = clampNumber(readValorantNumber(settings, "1o", 8), 0, 44);
    const outerThickness = clampNumber(readValorantNumber(settings, "1t", innerThickness), 1, 12);
    const outerOpacity = clampNumber(readValorantNumber(settings, "1a", 0.55), 0.08, 1);
    const dotEnabled = settings.d === "1" || (innerLength <= 1 && innerVertical <= 1);
    const dotSize = clampNumber(readValorantNumber(settings, "z", 2) * 2, 2, 16);

    const style = [
        `--vc-color:${color}`,
        `--vc-inner-length:${Math.max(innerLength, 1) * 5}px`,
        `--vc-inner-vertical:${Math.max(innerVertical, 1) * 5}px`,
        `--vc-inner-offset:${innerOffset * 4 + 4}px`,
        `--vc-inner-thickness:${innerThickness}px`,
        `--vc-inner-opacity:${innerOpacity}`,
        `--vc-outer-length:${outerLength * 4}px`,
        `--vc-outer-vertical:${outerVertical * 4}px`,
        `--vc-outer-offset:${outerOffset * 3 + 8}px`,
        `--vc-outer-thickness:${outerThickness}px`,
        `--vc-outer-opacity:${outerOpacity}`,
        `--vc-dot-size:${dotSize}px`
    ].join(";");

    return `
        <div class="valorant-crosshair-render" style="${style}">
            <span class="vc-axis vc-axis-x"></span>
            <span class="vc-axis vc-axis-y"></span>
            <span class="vc-line vc-inner vc-top"></span>
            <span class="vc-line vc-inner vc-right"></span>
            <span class="vc-line vc-inner vc-bottom"></span>
            <span class="vc-line vc-inner vc-left"></span>
            ${outerLength || outerVertical ? `
                <span class="vc-line vc-outer vc-top"></span>
                <span class="vc-line vc-outer vc-right"></span>
                <span class="vc-line vc-outer vc-bottom"></span>
                <span class="vc-line vc-outer vc-left"></span>
            ` : ""}
            ${dotEnabled ? '<span class="vc-dot"></span>' : ""}
        </div>
    `;
}

function parseValorantCrosshairCode(code) {
    const tokens = code.split(";").map((token) => token.trim()).filter(Boolean);
    const settings = {};
    for (let index = 0; index < tokens.length - 1; index += 2) {
        settings[tokens[index]] = tokens[index + 1];
    }
    return settings;
}

function getValorantCrosshairColor(settings) {
    if (settings.u && /^[0-9a-f]{6,8}$/i.test(settings.u)) {
        return `#${settings.u.slice(0, 6)}`;
    }

    const colors = {
        0: "#ffffff",
        1: "#20f7d2",
        2: "#50ff6b",
        3: "#dfff2f",
        4: "#132bff",
        5: "#27a8ff",
        6: "#ff4b50",
        7: "#ff8a1d",
        8: "#ff4fc3"
    };
    return colors[settings.c] || colors[settings.t] || "#20f7d2";
}

function readValorantNumber(settings, key, fallback) {
    const value = Number.parseFloat(settings[key]);
    return Number.isFinite(value) ? value : fallback;
}

function clampNumber(value, min, max) {
    return Math.min(Math.max(value, min), max);
}

async function copyValorantCode(code, item) {
    const cleanCode = code.trim();
    if (!cleanCode) {
        showValorantFeedback("Kopyalanacak kod yok.");
        return;
    }

    try {
        await navigator.clipboard.writeText(cleanCode);
    } catch {
        const textarea = document.createElement("textarea");
        textarea.value = cleanCode;
        textarea.setAttribute("readonly", "");
        textarea.style.position = "fixed";
        textarea.style.opacity = "0";
        document.body.appendChild(textarea);
        textarea.select();
        document.execCommand("copy");
        textarea.remove();
    }

    if (item) setValorantSelection(item);
    showValorantFeedback("Kod kopyalandı.");
}

function showValorantFeedback(message) {
    clearTimeout(valorantFeedbackTimer);
    valorantFeedback.textContent = message;
    valorantFeedback.classList.add("is-visible");
    valorantFeedbackTimer = setTimeout(() => valorantFeedback.classList.remove("is-visible"), 1700);
}

initValorantCrosshair();
